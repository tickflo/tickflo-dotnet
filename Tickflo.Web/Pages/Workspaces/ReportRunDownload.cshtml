@page "/workspaces/{slug}/reports/{reportId:int}/runs/{runId:int}/download"
@model Tickflo.Web.Pages.Workspaces.ReportRunDownloadModel
@{
    Layout = null; // This page returns FileResult; view not used
}
